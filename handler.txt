// package main

// import (
// 	"context"
// 	"fmt"

// 	"github.com/google/uuid"
// 	"github.com/jackc/pgx/v5"
// )

// type Record struct {
// 	ID        uuid.UUID `json:"id"`
// 	Name      string    `json:"name"`
// 	Details   string    `json:"details"`
// 	CreatedAt string    `json:"created_at"`
// }

// func getAllRecords(c context.Context, db *pgx.Conn) ([]Record, error) {

// 	// Fetch all records from the database
// 	rows, err := db.Query(c, "SELECT id, name, details, created_at FROM records")
// 	if err != nil {
// 		// log.Printf("Error fetching records: %v", err)
// 		return nil, fmt.Errorf("failed to execute query: %w", err)
// 		// return c.Status(500).SendString("Internal Server Error")
// 	}
// 	defer rows.Close()

// 	var records []Record

// 	for rows.Next() {
// 		var record Record
// 		if err := rows.Scan(&record.ID, &record.Name, &record.Details, &record.CreatedAt); err != nil {
// 			// log.Printf("Error scanning row: %v", err)
// 			return nil, fmt.Errorf("failed to scan row: %w", err)
// 			// return c.Status(500).SendString("Internal Server Error")
// 		}
// 		records = append(records, record)
// 	}

// 	if err := rows.Err(); err != nil {
// 		// log.Printf("Error iterating over rows: %v", err)
// 		return nil, fmt.Errorf("error iterating over rows: %w", err)
// 		// return c.Status(500).SendString("Internal Server Error")
// 	}

// 	return records, nil
// }
